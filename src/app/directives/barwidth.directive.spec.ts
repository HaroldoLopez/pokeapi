import { BarwidthDirective } from './barwidth.directive';

describe('BarwidthDirective', () => {
  it('should create an instance', () => {
    const directive = new BarwidthDirective();
    expect(directive).toBeTruthy();
  });
});
